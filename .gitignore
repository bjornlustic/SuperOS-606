.pio/
*.syx
*.hex
.DS_Store
.vscode/
docs/
.claude/
# the whole release tree stays out of git (kits ship outside the repo; the
# with-ROM builds especially: the D650C mask ROM is copyrighted and must
# never enter the repo)
release/
# personal ROM-embedded builds: generated header, copyrighted contents
src/d650/rom_embed.h
**/.DS_Store
